name: Build and deploy the WCAG Primer

on:
  push:
    branches:
      - main
      - master

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
      with:
        fetch-depth: 0
    - uses: ruby/setup-ruby@v1
      with:
        bundler-cache: true
    - run: |
        git config --global user.email "injms@users.noreply.github.com"
        git config --global user.name injms
        bundle exec rake publish
